---
# Minimal stub of the upstream Cluster API KubeadmConfigTemplate CRD, for
# envtest only. See test/crd/README.md. Never apply this to a real cluster.
#
# It reproduces only the identity of the resource (group, version, kind, plural,
# scope). That is all the reconciler needs: it reads metadata.name and
# metadata.finalizers, and evaluates the rule query against nested spec fields.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kubeadmconfigtemplates.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    kind: KubeadmConfigTemplate
    listKind: KubeadmConfigTemplateList
    plural: kubeadmconfigtemplates
    singular: kubeadmconfigtemplate
  scope: Namespaced
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          description: Test-only permissive stub of KubeadmConfigTemplate.
          type: object
          x-kubernetes-preserve-unknown-fields: true
